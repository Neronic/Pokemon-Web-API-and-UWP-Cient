<Page
    x:Class="PokemonClient.RegionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PokemonClient"
    xmlns:data="using:PokemonClient.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.Background>
            <ImageBrush Stretch="UniformToFill" ImageSource="Assets/poke53.jpg" Opacity="0.1"/>
        </Grid.Background>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel x:ConnectionId='2' x:Name="TitlePanel" Grid.Row="0" Margin="12,17,0,28">
            <TextBlock Text="Regions" FontSize="25" HorizontalAlignment="Center"/>
        </StackPanel>

        <GridView x:ConnectionId='3' Name="RegionList" Margin="30,0,0,0" 
                  Grid.Row="1"                                 
                  IsItemClickEnabled="True"
                  VerticalAlignment="Center"
                  HorizontalAlignment="Center">
            <GridView.ItemTemplate>
                <DataTemplate                         >
                    <StackPanel x:ConnectionId='8' Width="300">
                        <TextBlock x:ConnectionId='9'                           
                                   FontSize="20"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
        <ProgressRing x:ConnectionId='4' x:Name="progRing" Grid.Row="1" Width="60" Height="60"/>
        <RelativePanel Grid.Row="2">
            <Button x:ConnectionId='5' x:Name="btnRefresh" Content="Refresh" RelativePanel.AlignHorizontalCenterWithPanel="True"
                                             Margin="10,10,10,10"/>
            <Button x:ConnectionId='6' x:Name="btnBack" Content="Back" RelativePanel.AlignLeftWithPanel="False"                      
                    Margin="10,10,10,10"/>
            <Button x:ConnectionId='7' x:Name="btnAdd" Content="New Region" RelativePanel.RightOf="btnRefresh"
                                         Margin="10,10,10,10"/>
        </RelativePanel>
    </Grid>
</Page>

